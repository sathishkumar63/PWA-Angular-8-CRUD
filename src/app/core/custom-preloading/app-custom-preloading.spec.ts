import { AppCustomPreloading } from './app-custom-preloading';

describe('AppCustomPreloading', () => {
  it('should create an instance', () => {
    expect(new AppCustomPreloading()).toBeTruthy();
  });
});
